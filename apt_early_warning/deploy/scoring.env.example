# Copy to: /etc/apt_early_warning_scoring.env

# Bind address for uvicorn.
HOST=0.0.0.0
PORT=8000

# Model bundle path (flowFeatures default).
BUNDLE_PATH=/home/sealorl/BTLPY/apt_early_warning/model_out_flowfeatures/lgbm_flowfeatures_binary.joblib

# Optional policy overrides.
ALERT_LABELS=APT
ALERT_MIN_CONF_HIGH_IMPACT=0.50
ALERT_MIN_CONF_NON_BENIGN=0.50
